# Superseded pre-all-ancestor-and-recovery-hardening acceptance

This directory preserves the second fully executed Repair acceptance and its
complete content manifest.  It is not the final acceptance.  Subsequent
adversarial review added all-reachable-ancestor history reconciliation (including
the normal-push merge/second-parent attack), strict Run-manifest symlink checks,
inherited-socket descriptor closure evidence, plan-free registered Strategy
construction, qualification-fixture Final Holdout refusal, post-terminal Owner
workflow recovery, and immutable M3 qualification-authority checks.

The root-level acceptance artifacts supersede this directory only after the
complete gate set is rerun.  Nothing in this preserved epoch is treated as a
current PASS result.
